import { View, Text } from 'react-native'
type Props = {}
const refresh = (props: Props) => {
    return (
        <View>
            <Text>refresh</Text>
        </View>
    )
}
export default refresh